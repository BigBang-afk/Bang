# Optional: enables the "AI commentary" sanity-check on top of the deterministic
# candle/pattern/S-R engine. Without this set, the app still works fully —
# it just skips the Claude vision narrative.
ANTHROPIC_API_KEY=
