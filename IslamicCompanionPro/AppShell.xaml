<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:IslamicCompanionPro.Views"
       Title="Islamic Companion Pro"
       x:Class="IslamicCompanionPro.AppShell"
       Shell.FlyoutBehavior="Flyout"
       FlyoutBackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}">

    <!-- Splash + Onboarding are routed to, not shown in the flyout -->
    <ShellContent x:Name="SplashShellContent"
                  ContentTemplate="{DataTemplate views:SplashPage}"
                  Route="splash"
                  FlyoutItemIsVisible="False"
                  Shell.NavBarIsVisible="False" />

    <FlyoutItem Title="Home">
        <ShellContent ContentTemplate="{DataTemplate views:HomePage}" Route="home" />
    </FlyoutItem>

    <FlyoutItem Title="Quran">
        <ShellContent ContentTemplate="{DataTemplate views:QuranPage}" Route="quran" />
    </FlyoutItem>

    <FlyoutItem Title="Duas">
        <ShellContent ContentTemplate="{DataTemplate views:DuaCategoriesPage}" Route="duacategories" />
    </FlyoutItem>

    <FlyoutItem Title="Qibla Finder">
        <ShellContent ContentTemplate="{DataTemplate views:QiblaPage}" Route="qibla" />
    </FlyoutItem>

    <FlyoutItem Title="Prayer Times">
        <ShellContent ContentTemplate="{DataTemplate views:PrayerTimesPage}" Route="prayertimes" />
    </FlyoutItem>

    <FlyoutItem Title="Islamic Calendar">
        <ShellContent ContentTemplate="{DataTemplate views:IslamicCalendarPage}" Route="islamiccalendar" />
    </FlyoutItem>

    <FlyoutItem Title="Tasbeeh Counter">
        <ShellContent ContentTemplate="{DataTemplate views:TasbeehPage}" Route="tasbeeh" />
    </FlyoutItem>

    <FlyoutItem Title="Settings">
        <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" Route="settings" />
    </FlyoutItem>

    <FlyoutItem Title="About">
        <ShellContent ContentTemplate="{DataTemplate views:AboutPage}" Route="about" />
    </FlyoutItem>

</Shell>
