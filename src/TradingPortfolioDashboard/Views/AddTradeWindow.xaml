<Window x:Class="TradingPortfolioDashboard.Views.AddTradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trade" Height="480" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Symbol" Style="{StaticResource KpiLabel}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Text="{Binding Symbol, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Side" Style="{StaticResource KpiLabel}" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="3" ItemsSource="{Binding Sides}" SelectedItem="{Binding Side}" Margin="0,0,0,12"/>

        <Grid Grid.Row="4" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Quantity" Style="{StaticResource KpiLabel}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Price" Style="{StaticResource KpiLabel}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="5" Text="Fees" Style="{StaticResource KpiLabel}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="6" Text="{Binding Fees, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="7" Text="Notes" Style="{StaticResource KpiLabel}" Margin="0,0,0,4" VerticalAlignment="Top"/>
        <TextBox Grid.Row="7" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalAlignment="Stretch" Margin="0,24,0,12"/>

        <TextBlock Grid.Row="8" Text="{Binding ErrorMessage}" Foreground="{StaticResource Brush.Negative}" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" Margin="0,0,8,0"/>
            <Button Content="Save" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
