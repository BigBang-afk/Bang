import { ScrollText } from "lucide-react";
import { ComingSoon } from "@/components/layout/coming-soon";

export default function GoldLedgerPage() {
  return (
    <ComingSoon
      icon={ScrollText}
      title="Gold Ledger"
      description="Gold-weight accounting across the business. Arriving in the next phase."
    />
  );
}
