namespace RoyaleClash.Cards
{
    /// <summary>Broad category of a card; determines which definition subclass it uses.</summary>
    public enum CardType
    {
        Troop = 0,
        Building = 1,
        Spell = 2,
    }
}
