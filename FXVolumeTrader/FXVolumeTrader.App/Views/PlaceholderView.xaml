<UserControl x:Class="FXVolumeTrader.App.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource PageTitleTextStyle}" Margin="0,0,0,16" />

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" VerticalAlignment="Top">
            <StackPanel MaxWidth="640">
                <TextBlock Text="This page is not implemented yet." Style="{StaticResource SectionTitleTextStyle}" />
                <TextBlock Text="{Binding PhaseNote}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />

                <TextBlock Text="{Binding ExtraNote}" TextWrapping="Wrap" Margin="0,16,0,0"
                           Foreground="{StaticResource WarningBrush}" FontSize="12" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
