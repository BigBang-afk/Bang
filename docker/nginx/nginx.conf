# Production reverse proxy: routes API/SignalR traffic to the backend container and everything
# else to the static frontend container. This is the single public entry point in docker-compose.
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    upstream flexxsignal_api {
        server api:8080;
    }

    upstream flexxsignal_client {
        server client:80;
    }

    server {
        listen 80;
        server_name _;

        client_max_body_size 50m;

        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;

        location /api/ {
            proxy_pass http://flexxsignal_api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /hubs/ {
            proxy_pass http://flexxsignal_api;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_read_timeout 3600s;
        }

        location /health {
            proxy_pass http://flexxsignal_api/health;
            proxy_http_version 1.1;
        }

        location /swagger/ {
            proxy_pass http://flexxsignal_api;
            proxy_http_version 1.1;
        }

        location / {
            proxy_pass http://flexxsignal_client;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }
    }
}
